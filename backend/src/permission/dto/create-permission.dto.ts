export class CreatePermissionDto {
    id: number;

    viewOrder: boolean

    sendOrder: boolean

    viewDrawing: boolean

    sendDrawing: boolean

    takeResource: boolean

    repairing: boolean
}